
docker pull hello-world
docker run hello-world

docker run -it ubuntu 
docker stop <id>

docker network ls
docker network create mongo-network

docker run -d -p 27017:27017 --name mongo --network mongo-network -e MONGO_INITDB_ROOT_PASSWORD = admin mongo 

docker network inspect mongo-network // to see which container are connected to mongo-network

docker run -d -p 8081:8081 --name mongo-express --network mongo-network -e MF_CONFIG_MONGODB_ADMINUSERNAME = admin -e MF_CONFIG_MONGODB_ADMINPASSWORD =admin -e 
MF_CONFFIG_MONGODB_URL= "mongodb://admin:admin@mongo:27017" mongo-express




